# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# Runtime environment variables read during local development and local tests.
# In production, all of the values defined in these files are required to
# be defined via environment variables
/config/*.secrets.exs

# Postgres volume used by Docker
/db

# Temporary files
/tmp

# Ex language server
.elixir_ls

# Text editors
.idea
.vscode
*.iml

# Temp files
.DS_Store
*.iml

# Dialyzer Persistent Lookup Table (PLT) files
# See: https://www.erlang.org/doc/apps/dialyzer/dialyzer_chapter.html
/priv/plts
/priv/plts

# Artifact storage when using a local file store
/priv/turbo_artifacts
# When using the docker-compose example for production
# this is used to demonstrate how to map a volume to the container path
# which can also be storated locally with the compose file for easy backup
/docker_turbo_artifacts
# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
turbo-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/

